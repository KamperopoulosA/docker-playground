version: "3.0"

services:
  web-app:
    image: nginx
    ports:
      - "80:80"
    volumes:
      - ./data:/usr/share/nginx/html
  util:
    image: kaan/util
    depends_on:
      - web-app
    command: "curl http://web-app"